import React from 'react';
import {createRoot} from 'react-dom/client';
import {Workflow,FileDiff,FlaskConical,CalendarClock,Boxes,Check,Info,ArrowRight,Pause} from 'lucide-react';
import './preview.css';
const agents=[{name:'Coordinator',icon:Workflow,state:'Coordinating',status:'running'}, {name:'Change impact',icon:FileDiff,state:'Task complete',status:'completed'}, {name:'Validation',icon:FlaskConical,state:'Checking evidence',status:'running'}, {name:'Program & commercial',icon:CalendarClock,state:'Assessing timing',status:'running'}, {name:'Manufacturing',icon:Boxes,state:'Not invoked',status:'neutral'}];
function Node({a}:any){return <button className={'agent '+a.status} aria-label={a.name+' · '+a.state}><span className="ring"><a.icon size={26} strokeWidth={1.6}/>{a.status==='completed'&&<Check className="check" size={15}/>}</span><strong>{a.name}</strong><span>{a.state}</span></button>}
function Concept({stage}: {stage:boolean}){return <section className={'workspace '+(stage?'stage':'ribbon')}><header><h2>Agent workspace</h2><span>Observed work <Info size={16}/></span></header><div className="scope"><span>CR-017 · Running task</span><small>Illustrative concept</small></div>{stage?<div className="stage-body"><Node a={agents[0]}/><svg viewBox="0 0 800 55" aria-label="Three illustrated participants"><path d="M400 0V24M100 55V24H500V55M300 55V24"/></svg><div className="specialists">{agents.slice(1).map(a=><Node key={a.name} a={a}/>)}</div></div>:<div className="ensemble">{agents.map(a=><Node key={a.name} a={a}/>)}</div>}<footer><span><Pause size={17}/> Two specialist tasks in parallel</span><ArrowRight size={18}/></footer></section>}
createRoot(document.getElementById('root')!).render(<main><div className="brand">STRATOS SILICON <span>Illustrative design exploration · No live telemetry</span></div><h1>Two ways to put the agents first.</h1><p className="label">A · Agent stage</p><Concept stage/><p className="label">B · Agent ensemble</p><Concept stage={false}/></main>);
