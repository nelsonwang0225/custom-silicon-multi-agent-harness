# Autonomous opening — deterministic browser evidence

All images come from isolated local source/host services and deterministic SDK
agents. They demonstrate actual persisted workflow/checkpoint/UI behavior, not a
paid AI run or real human approval. Speech is mocked. No working demo is reset.

- `idle-1440.png`: no published event or fabricated participant activity.
- `investigating-viewport-1440.png`: actual viewport after the explicit Open Overview
  link focuses Agent Workspace; three observed specialists under the Coordinator.
- `investigating-1440.png`: complete Overview during persisted investigation.
- `handoff-1440.png`, `handoff-1920.png`, `handoff-1280.png`, `handoff-390.png`:
  Program Owner handoff, no moving work, QE-011's own 100-unit scope.
- `case-1440.png`: connected QE-011 case with exact source-bound recovery plan.
- `manufacturing-1440.png`: real source records, 88/100 first-pass passes, 100 held,
  zero released/eligible; root cause not established.
- `operations-1440.png`: Manufacturing event ID/version, actual run and scoped RAG.

Screenshots are static; Playwright separately samples CSS offset distance to prove
that active dots move, and checks that completion/idle/stale/reduced-motion stops
animation. The tests also perform reset and replay, assert no pre-handoff attention,
and verify navigation cannot publish or run the event.

See [handoff](../../PHASE10_AUTONOMOUS_OPENING_HANDOFF.md) and
[verification](../../PHASE10_AUTONOMOUS_VERIFICATION.json).
