# Phase 09.4 startup review

These four 1440×900 screenshots were captured and visually inspected from isolated scripted startup testing. The live adapter was configured but never invoked. No paid model, actual human approval or real microphone is represented. The complete baseline, persistence, reset and network evidence is in [the cold-start record](../../PHASE09_4_COLD_START.json).

| Surface | Screenshot |
|---|---|
| Canonical baseline Overview | [View](overview-1440.png) |
| Helios with all four new cases | [View](PRG-A17-1440.png) |
| Operations with no current demo runs | [View](operations-1440.png) |
| Deliberately blocked lazy asset, with recovery controls | [View](workspace-load-failure-1440.png) |

The asset-failure screenshot is an injected browser failure, not a normal-route error or a backend failure. The same test restores the asset and verifies reload recovery. Source baseline facts, current run history and historical eval results remain separate.
