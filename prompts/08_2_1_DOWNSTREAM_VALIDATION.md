Continue working in the existing Stratos Silicon repository and current
Phase 08 thread.

Implement Phase 08.2.1:
DOWNSTREAM PHYSICAL VALIDATION COMPLETION FOR CR-017.

This is a narrow extension of the already-working connected CR-017 flow.

Do not redesign the UI.
Do not change agent prompts or Phase 06 approval/execution authority.
Do not implement the other demo workflows yet.
Do not build scheduling/automation infrastructure yet.
Do not expand source-system write permissions beyond what is explicitly
authorized below.

Git remains relaxed:
- no commit/push work
- preserve unrelated local work
- avoid destructive operations

==================================================
1. OBJECTIVE
==================================================

Complete the downstream portion of the CR-017 demo story after the existing:

investigation
→ human approval
→ governed execution
→ validation job scheduled
→ source readback verified

The current UI correctly ends with:

Physical validation:
Results / review pending

Add a controlled synthetic downstream sequence so the demo can continue:

scheduled validation job
→ synthetic lab result arrives
→ control plane observes the result
→ validation evidence is reassessed
→ engineering review becomes ready
→ engineering approver reviews the result
→ CR-017 validation evidence is closed
→ customer acceptance remains separate and pending

Important:

The application must NOT pretend that an agent physically performed a
semiconductor validation test.

The synthetic test result represents an external/downstream lab event in the
mock Validation Lab system.

==================================================
2. PRESERVE THE EXISTING BOUNDARIES
==================================================

Keep these distinctions explicit:

Scheduling a job
≠ physical testing

Physical test completed
≠ test passed

Test passed
≠ evidence applicable

Evidence applicable
≠ engineering approval

Engineering approval
≠ customer acceptance

The agent may:
- observe a new result
- retrieve evidence
- reassess applicability
- explain whether the previous evidence gap is now addressed
- prepare engineering review

The agent must NOT:
- fabricate a test result
- modify pass criteria
- claim physical execution without a source record
- approve engineering adequacy itself
- grant customer acceptance

==================================================
3. USE THE EXISTING VALIDATION LAB
==================================================

Inspect the current mock Validation Lab data model, APIs, UI and job/result
contracts before implementing.

Reuse existing models/endpoints wherever possible.

Add the smallest supported mechanism for a synthetic downstream validation
result to be recorded for the already-scheduled CR-017 validation job.

Do NOT build a new parallel lab service.

The synthetic result must be linked to:
- the existing validation job
- CR-017
- the exact approved validation plan
- the correct hardware/configuration
- the relevant software/workload version
- the test criteria version
- the result timestamp/version
- the source provenance

==================================================
4. DEMO-ONLY RESULT ARRIVAL
==================================================

Provide a clearly labeled demo control in the mock Validation Lab UI such as:

Publish synthetic result

or:

Simulate lab completion

Use whichever wording best matches existing app conventions.

The control must be clearly labeled as demo/synthetic.

Do NOT place this control in the Stratos control-plane approval area as though
the control plane itself ran the lab test.

Prefer the action to live in the Validation Lab source application.

The action should:
- require an already-existing scheduled CR-017 job
- create/update only the supported synthetic result record
- be idempotent
- avoid duplicate result creation
- preserve the scheduled job history
- record that the result was generated by a demo fixture/action

Do not automatically trigger this action after scheduling.

==================================================
5. SYNTHETIC CR-017 RESULT
==================================================

Use the actual CR-017 approved validation scope and fixture data already in
the repository.

Do not invent arbitrary new performance thresholds.

The synthetic result should represent a successful completion of the exact
validation campaign needed to close the previously identified CR-017 evidence
gap.

At minimum, the source record should support determining:

- job completed
- required test scope executed
- correct configuration
- correct software/workload version
- relevant context/workload bins completed
- criteria evaluation result
- applicability to REQ-042-V2
- no evidence mismatch

Use existing criteria/result conventions.

If the repository already contains an appropriate completed result fixture,
reuse/adapt it rather than creating a conflicting parallel result format.

==================================================
6. RESULT STATES
==================================================

Support truthful downstream states such as:

SCHEDULED
RUNNING / IN_PROGRESS if already modeled
COMPLETED
FAILED
RESULT_AVAILABLE

Do not introduce fake intermediate timing merely for animation.

For the primary demo path, the explicit synthetic action may transition the
fixture from scheduled to completed/result available in one user action.

That is acceptable as long as the UI makes clear that this is a demo event,
not an elapsed real-world lab execution.

==================================================
7. CONTROL-PLANE RESULT DETECTION
==================================================

After the Validation Lab result exists, the CR-017 control-plane case should
be able to observe it through the existing source-read architecture.

Do not require users to manually copy IDs.

A Refresh workflow / Refresh sources action may retrieve the result.

If a safe existing source-event mechanism already exists, reuse it.

Do NOT introduce a production event bus or continuous background monitor in
this slice.

Do not trigger model calls merely by normal polling/navigation.

==================================================
8. ADD A DOWNSTREAM REASSESSMENT ACTION
==================================================

Once the new source result exists, expose a CR-017 action such as:

Assess validation result

or:

Reassess evidence

This should use the existing connected workflow architecture rather than a
frontend-only calculation.

The reassessment should focus on:

- was the expected job completed?
- does the result match the approved validation plan?
- does the result apply to the exact requested configuration/workload?
- were the required criteria satisfied?
- does this close the original evidence gap?
- what remains for human engineering review?

Reuse the existing Validation & Evidence specialist and coordinator.

Do not create a new result-analysis agent solely for this step.

==================================================
9. AGENT OUTPUT AFTER RESULT ARRIVAL
==================================================

For the successful synthetic result, the expected conclusion should be
equivalent to:

Applicable validation evidence has now been received for the requested
CR-017 workload and configuration.

The previously identified validation evidence gap is closed.

Engineering review is ready.

Do NOT allow the agent to state:

- hardware approved
- silicon qualified
- customer accepted
- shipment approved

unless the relevant human/source records explicitly establish those states.

==================================================
10. ENGINEERING REVIEW
==================================================

Add a distinct human engineering review after evidence reassessment.

This is NOT the same decision as the earlier approval to schedule validation.

The earlier decision was:
"May we execute this validation plan?"

The new decision is:
"Does the completed and applicable validation evidence satisfy the engineering
requirement for CR-017?"

Reuse the existing proposal/decision governance model where practical rather
than building another approval engine.

If the current Phase 06 contract can support a new decision type cleanly,
reuse it.

The engineering review record should bind:
- CR-017
- requirement/configuration
- exact validation result/source version
- relevant criteria/evidence
- review version/digest where appropriate
- trusted engineering approver
- approve/reject decision
- timestamp/comment

Do not allow the Program Operator to substitute for the Engineering Approver.

The Program Operator may coordinate or initiate reassessment.
The Engineering Approver owns the consequential engineering decision.

==================================================
11. ENGINEERING REVIEW AUTHORITY
==================================================

Use the existing trusted demo identity model.

Expected role behavior:

Program Operator:
- may initiate investigation/reassessment
- may inspect evidence/status
- cannot grant engineering approval

Engineering Approver:
- may perform the engineering evidence review if allowed by existing policy

Wrong role:
- review rejected
- zero engineering-state mutation

Do not infer authority from a browser-supplied role string.

Keep the visible Demo identity label.

==================================================
12. POST-REVIEW BUSINESS STATE
==================================================

After a successful engineering review, represent the state truthfully.

Expected successful demo state:

Validation job:
Completed

Validation result:
Received

Evidence applicability:
Confirmed

Validation evidence:
Complete

Engineering review:
Approved

CR-017 technical state:
Validation complete / engineering-approved
(use existing terminology where possible)

Customer acceptance:
Pending / separate

Customer commitment:
Unchanged

Do NOT automatically mark customer acceptance or commercial delivery approved.

==================================================
13. UPDATE THE LIFECYCLE UI
==================================================

The existing lifecycle currently ends at:

7. Physical validation

Extend/refine that section without making the page overly complex.

Recommended behavior:

Request
→ Evidence review
→ Recommendation
→ Human decision
→ Execution
→ Verification
→ Physical validation
→ Engineering review

If changing the visible count from 7 to 8 harms the current layout, an
acceptable alternative is to make Physical validation contain substates:

Scheduled
→ Results received
→ Engineering review

Use actual records to determine completed/current states.

Do not mark future states complete until supported.

==================================================
14. PHYSICAL VALIDATION PANEL
==================================================

Add a focused downstream panel on CR-017.

Display actual source-backed information such as:

Validation job
Job ID

Status
Scheduled / Completed

Validation operation owner
if actually modeled

Approved validation option

Configuration

Result
Pending / Received / Failed

Evidence applicability
Pending / Confirmed / Gap remains

Engineering review
Not ready / Ready / Approved / Rejected

Provide actions where supported:

Open Validation Lab
View validation result
Assess validation result
Review validation evidence

Do not show actions the current state cannot support.

==================================================
15. SOURCE APP DEEP LINK
==================================================

"Open Validation Lab" should take the user to the relevant source-app context
for the scheduled job/result.

If deep linking to the exact job is supported, use it.

Otherwise open Validation Lab with sufficient context/search state.

Do not expose arbitrary query injection or generic source access.

==================================================
16. RESULT EVIDENCE MODAL
==================================================

Reuse the existing contextual evidence modal.

For a completed result, show:

Left:
- structured Validation Lab result or available source document

Right:
- job/configuration/workload
- criteria/version
- result summary
- applicability
- provenance
- timestamp/version
- whether engineering review has occurred

Do not fabricate a PDF when the source is structured data.

==================================================
17. FAILURE / ALTERNATE STATES
==================================================

Build the downstream path to handle more than the successful fixture.

Support deterministic test states for:

- result not yet available
- completed result fails criteria
- result applies to wrong software version
- result applies to wrong configuration
- result incomplete
- conflicting result
- engineering review rejected
- result becomes stale/superseded

Expected behavior:

Failed or inapplicable validation evidence
must NOT close the original evidence gap.

The system may:
- explain why
- recommend additional investigation
- keep CR-017 open/escalated

Do not automatically schedule another test without a new governed decision.

==================================================
18. REUSE PHASE 07 RELIABILITY PRINCIPLES
==================================================

Preserve the tested reasoning boundaries:

- wrong-version evidence is not applicable
- missing evidence is unknown, not failed
- correlation is not root cause
- successful source write is not verification
- scheduled is not passed
- passed result is not customer acceptance

Add relevant deterministic eval/test coverage for the new downstream states.

Do not change existing Phase 07 grader behavior unnecessarily.

==================================================
19. CONTROL-PLANE OVERVIEW INTEGRATION
==================================================

Reflect the actual downstream CR-017 state on Overview.

Examples:

Before result:
CR-017 · Physical validation pending

After result received:
CR-017 · Engineering review ready

After engineering approval:
CR-017 · Validation complete
Customer acceptance pending

Do not remove CR-017 from Needs attention if another consequential step remains.

Do not label the entire program On track solely because this single case reached
engineering approval unless existing program-health rules support it.

==================================================
20. DECISIONS PAGE INTEGRATION
==================================================

Show the new engineering-evidence review as a separate decision from the
earlier validation-plan approval.

The UI should make the distinction obvious.

Example:

Decision 1
Authorize validation execution
Status: Approved / executed

Decision 2
Review completed validation evidence
Status: Needs review

Do not overwrite Decision 1 with Decision 2.

Maintain history.

==================================================
21. ALERTS
==================================================

If the existing alert feed supports it, add source-backed alerts such as:

Validation result received — engineering review ready

or

Validation result failed — CR-017 remains blocked

Only when such records actually exist.

No invented "AI detected" language if a source refresh found the record.

Marking an alert read must not approve anything.

==================================================
22. CONCIERGE CONTEXT
==================================================

Do not fully connect freeform Concierge yet.

Update contextual suggested questions so CR-017 after result arrival can show:

- Did the validation result close the evidence gap?
- What remains before engineering approval?
- Why is customer acceptance still pending?

If replies remain Preview, keep that clearly labeled.

Voice input remains draft dictation only.

==================================================
23. HOST/API SURFACE
==================================================

Add only the minimal typed host interfaces necessary for:

- read validation job/result
- trigger the demo-only synthetic result through the mock Validation Lab
  if source-app routing requires it
- run downstream reassessment
- read/review engineering-evidence decision
- read updated case state

The control plane should preferably not expose a generic
"create validation result" endpoint.

The synthetic-result action belongs to the Validation Lab demo/source app.

No arbitrary tool/write proxy.

==================================================
24. IDEMPOTENCY
==================================================

The synthetic result action must be retry-safe.

Repeated click/reload must not:
- create duplicate results
- create duplicate engineering review decisions
- duplicate activities

Use existing IDs/idempotency conventions where available.

A deliberate reset/new demo scenario should use explicit fixture reset tooling,
not accidental duplicate writes.

==================================================
25. AUTOMATED TESTS
==================================================

Add tests covering at least:

A. Cannot publish result without an existing scheduled job.

B. Synthetic result is clearly marked as demo-generated.

C. Repeated publish does not create duplicate result.

D. Correct result is retrievable through Validation Lab source interfaces.

E. Wrong version/configuration remains inapplicable.

F. Passing result does not automatically grant engineering approval.

G. Program Operator cannot grant engineering approval.

H. Engineering Approver can review the exact evidence when policy allows.

I. Review binds exact result/evidence version.

J. Stale/superseded result invalidates old review where policy requires it.

K. Reassessment closes the gap only when evidence is applicable.

L. Failure result keeps case open.

M. Engineering approval does not set customer acceptance.

N. Overview/Decisions reflect the new state correctly.

O. No duplicate job or Planner action is created during downstream reassessment.

P. Existing Phase 06 execution path remains unchanged.

Q. Existing 40/40 offline eval suite remains green.

==================================================
26. BROWSER / UI TESTING
==================================================

Use deterministic fixtures.

Test:

1. Existing CR-017 after execution shows Physical validation pending.

2. Open Validation Lab.

3. Publish synthetic result.

4. Return/refresh control plane.

5. Result becomes available.

6. Run Assess validation result.

7. Evidence gap closes for the successful fixture.

8. Engineering review decision appears.

9. Program Operator cannot approve.

10. Engineering Approver can approve.

11. Case moves to validation complete / engineering-approved state.

12. Customer acceptance remains pending.

13. Failed-result deterministic fixture behaves correctly.

Do not use paid agents in automated tests.

==================================================
27. LIVE / PAID MODEL BEHAVIOR
==================================================

Do not automatically run paid model calls.

Provide a manual optional connected command/path for:

actual CR-017 downstream reassessment using the configured OpenAI runtime.

The synthetic Validation Lab result itself is source data and should not require
a model call merely to exist.

==================================================
28. DOCUMENTATION
==================================================

Update Phase 08 documentation with:

- downstream validation lifecycle
- synthetic-result boundary
- source-system ownership
- reassessment flow
- engineering-review authority
- difference between execution approval and engineering evidence approval
- final CR-017 business-state semantics
- manual demo instructions

Explicitly state:

Synthetic lab completion is a demo event in the mock Validation Lab.

The agents do not physically perform the test.

==================================================
29. FINAL REPORT
==================================================

When complete, report:

1. Existing Phase 08.2 components reused
2. Validation Lab changes
3. Synthetic result implementation
4. Reassessment behavior
5. Engineering decision implementation
6. Exact authority boundary
7. UI changes
8. Files changed
9. Tests/results
10. Phase 07 regression preservation
11. Manual demo click path
12. Optional paid/live reassessment not run
13. Known limitations
14. Exact next 08.3 step

==================================================
30. STOPPING POINT
==================================================

Stop after Phase 08.2.1 implementation and verification.

Do not continue into:
- 08.3 Workflows & Automations
- yield/quality workflow implementation
- delivery-readiness workflow implementation
- touchless change implementation
- fully connected Concierge
- active recurring schedulers
- Phase 09 cosmetic polish
- Git cleanup/push

08.2.1 is complete when the app can demonstrate:

approved validation plan
→ scheduled Validation Lab job
→ explicit synthetic lab completion
→ result observed by control plane
→ agent reassessment
→ human engineering review
→ validation evidence complete
→ customer acceptance still pending